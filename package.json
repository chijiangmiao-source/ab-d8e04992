{
  "name": "barcode-subpool-audit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "多重测序条码子库审计：Web Worker 内精确求解最大权独立集（优先权和 → 记录数），任意精度同优计数与必选/可选/从不选归属。",
  "scripts": {
    "start": "node src/server/server.js",
    "test": "node --test test/",
    "verify": "node scripts/verify.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
